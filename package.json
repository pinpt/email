{
	"name": "@pinpt/email",
	"version": "0.0.3",
	"description": "Email templates for Pinpoint",
	"main": "index.js",
	"types": "index.d.ts",
	"repository": {
		"type": "git",
		"url": "https://github.com/pinpt/email.git"
	},
	"scripts": {
		"test": "jest --config=jest.config.js",
		"test:ci": "jest --ci --config=jest.config.js --reporters='default' --reporters='./.github/github-actions-reporter'",
		"test:write": "npm run test -- --updateSnapshot",
		"format": "prettier --write ."
	},
	"dependencies": {
		"handlebars": "^4.7.7"
	},
	"devDependencies": {
		"@babel/preset-env": "^7.15.0",
		"babel": "^6.23.0",
		"jest": "^27.0.6"
	}
}
